export type PostInfo = {
    userName: string
    profileImg: string
    postImg: string
}

export type User = {
    id: number,
    user_name: string,
    profile_img: string,
}

export type MyPost = {
    img: string
}
